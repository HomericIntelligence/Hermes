{
  "follow_ups": [
    {
      "category": "core",
      "title": "ADR-001 Decision section still describes restart-only stance",
      "body": "ADR-001:29-40 (Decision section) states 'Reconnection to NATS is handled only at startup' and describes ending with 'The operator is alerted and must restart Hermes or restore NATS connectivity.' This contradicts ADR-002 which documents the runtime _reconnect_loop. Update to note both startup retries AND runtime reconnection are now in place."
    },
    {
      "category": "core",
      "title": "ADR-001 Rationale section references outdated startup-only design",
      "body": "ADR-001:52-54 claims 'Startup retries handle this without compromising the runtime contract.' This rationale assumes no runtime reconnection exists. Update to acknowledge startup retries handle boot-time synchronization while the runtime loop handles operational resilience for transient drops."
    }
  ],
  "rejected": [
    {
      "title": "Add integration test for NATS restart recovery",
      "reason": "Integration test already exists at tests/test_integration.py:773-780; adding more integration tests is out-of-scope for this documentation fix."
    },
    {
      "title": "Document reconnect loop in code comments",
      "reason": "Code comments at publisher.py:146-154 already document the callback design and _reconnect_loop; this is documentation polish, not a core defect."
    }
  ]
}
