```json
{
  "follow_ups": [
    {
      "category": "safety",
      "title": "Apply polling helper to remaining 8 integration tests with race condition",
      "body": "tests/test_integration.py contains 8 additional callback-waiting tests that share the same sleep(0.1/0.2) race condition as the 3 fixed in #428: test_publish_agent_event_delivers_message:93, test_webhook_to_nats_end_to_end:204, test_large_payload:363, test_publish_includes_schema_version:539, test_publish_schema_version_present_in_task_event:566, test_loop_resets_backoff_when_connection_healthy:721, test_publish_after_reconnect_succeeds:941, test_request_id_propagated_to_nats_message:1067. Under full-suite CPU load, these will flake identically to the 3 just fixed. The helper wait_for_messages() is already implemented and tested; the follow-on is mechanical application to these 8 call sites, each a straightforward sleep→wait_for_messages replacement with expected count inferred from assertions."
    }
  ],
  "rejected": []
}
```
